abstract final class AppConstants {
  static const appName = 'Origami';
  static const maxContentWidth = 440.0;
  static const defaultHorizontalPadding = 20.0;
  static const defaultAnimationDuration = Duration(milliseconds: 280);
}
